spring.cloud.gcp.sql.enabled=false
spring.cloud.gcp.config.enabled=false
spring.cloud.gcp.core.enabled=false
spring.cloud.gcp.storage.enabled=false
spring.cloud.gcp.secretmanager.enabled=false

spring.datasource.url=${MYSQL_URL:jdbc:tc:mysql:8.0.33://localhost/mypage?tinyInt1isBit=false}
spring.datasource.username=${MYSQL_USER:mypage}
spring.datasource.password=${MYSQL_PASS:mypage}
spring.liquibase.change-log=classpath:/db/changelog/db.changelog-root.yaml
spring.jpa.hibernate.ddl-auto=none

google.clientid=dummy
slack.hyttekanal.id=dummy
slack.utlysning.channel=dummy
gcs.bucket.name=dummy

LIQUIBASE_ENABLED=true
OAUTH_ISSUER_URI=http://localhost:8099/default

# Enable X-Test-User-Id header authentication for integration tests ONLY
# WARNING: This MUST NEVER be enabled in production!
security.test-auth.enabled=true