<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd">

    <changeSet id="create-availability-history-table" author="claude">
        <createTable tableName="availability_history">
            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="consultant_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>
            <column name="from_status" type="VARCHAR(50)">
                <constraints nullable="true"/>
            </column>
            <column name="to_status" type="VARCHAR(50)">
                <constraints nullable="false"/>
            </column>
            <column name="changed_at" type="DATETIME">
                <constraints nullable="false"/>
            </column>
            <column name="changed_by_id" type="BIGINT">
                <constraints nullable="true"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="availability_history"
                baseColumnNames="consultant_id"
                constraintName="fk_availability_history_consultant"
                referencedTableName="user"
                referencedColumnNames="id"/>

        <addForeignKeyConstraint
                baseTableName="availability_history"
                baseColumnNames="changed_by_id"
                constraintName="fk_availability_history_changed_by"
                referencedTableName="user"
                referencedColumnNames="id"/>

        <createIndex indexName="idx_availability_history_consultant" tableName="availability_history">
            <column name="consultant_id"/>
        </createIndex>

        <createIndex indexName="idx_availability_history_changed_at" tableName="availability_history">
            <column name="changed_at"/>
        </createIndex>

        <comment>Create availability_history table to track consultant status changes over time for analytics</comment>
    </changeSet>

</databaseChangeLog>
